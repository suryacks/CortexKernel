apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-storage
spec:
  selector:
    app: {{ .Release.Name }}-storage
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.storage.port }}
  type: {{ .Values.service.type }}
